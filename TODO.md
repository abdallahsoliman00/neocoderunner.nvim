# TODO
- Make the commands configurable by the user.
    * Do this by making a small parser to decode the runner commands.
- Add a feature that allows the user to configure their environment such as export variables, run scripts or activate a virtual envronment.

## Session milestones:
- Make a parser that takes the runners.json file and returns a runners table similar to the one in languages.lua
