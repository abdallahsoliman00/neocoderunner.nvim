# TODO
- Add a feature that allows the user to configure their environment such as export variables, run scripts or activate a virtual envronment.
- Add environment variable exporting
- Edit the README to acccount for the changes.
- Make a logo
- Add custom commands or scripts that can be run before the run commands run.
